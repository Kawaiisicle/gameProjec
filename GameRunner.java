
/**
 * Write a description of class GameRunner here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
import java.util.List;
import java.util.ArrayList;
import java.util.Scanner;
public class GameRunner
{
    /**
     *  This class is broken/outdated. Pay no attention to it.
     */
    // public static void main()
    // {
        
        // Backpack pack = new Backpack();
        // //System.out.println("Well, " + name + ", here are a few things to take with you on your adventure!");
        
        // pack.storeItem(new Consumable("bread", 1, 5));
        // pack.storeItem(new Consumable("bread", 1, 5));
        // pack.storeItem(new Consumable("water", 2, 3));
        // pack.storeItem(new Consumable("health potion", 2, 25));
        // pack.storeItem(new Melee("hammer", 5, 10, 50));
        // pack.storeItem(new Ranged("bow", 5, 10, 1));
        // pack.storeItem(new Arrow());
        // pack.storeItem(new Arrow());
        // pack.storeItem(new Arrow());
        // pack.storeItem(new Arrow());
        // pack.storeItem(new Arrow());
        // pack.storeItem(new Arrow());
        // pack.storeItem(new Arrow());
        // pack.storeItem(new Arrow());
        // pack.storeItem(new Arrow());
        // pack.storeItem(new Arrow());
        // pack.storeItem(new Arrow());
        // pack.storeItem(new Arrow());
        // Arrow specialArrow = new Arrow();
        // pack.storeItem(specialArrow);
        
        // System.out.println("The following items have been added to your inventory:");
        // System.out.println();
        // pack.getInventory();
        // System.out.println();
        
        
        
        // System.out.println("You now decide to attack yourself... for some reason.");
        // player.attack((Weapon)pack.getItem("hammer"), player);
        // player.attack((Weapon)pack.getItem("hammer"), player);
        // player.attack((Weapon)pack.getItem("hammer"), player);
        // player.attack((Weapon)pack.getItem("hammer"), player);
        // player.takeDamage(0-(player.getHealth()-player.getMaxHP()));
        // UnfortunateGoblin steve = new UnfortunateGoblin();
        
        // steve.listDebuffs();
        
    // }
}
